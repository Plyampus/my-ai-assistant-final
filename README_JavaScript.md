# 🤖 AI Assistant (JavaScript Edition)

Персональний AI помічник з веб-інтерфейсом та мобільною підтримкою.

## 📦 Архітектура

```
+-----------+          +-----------+
|  Next.js  |  HTTP   |  Node.js  |
| Frontend  |-------->| Backend   |
| (React)   |         | (Express) |
+-----------+          +-----------+
                            |
                            v
                       Google Gemini API
                       + Local Memory (JSON)
```

## 🚀 Встановлення

### 1. Backend (Node.js + Express)

```bash
cd backend
npm install
```

Налаштуйте `.env`:
```
GOOGLE_API_KEY=your_api_key_here
PORT=5000
```

Запустіть:
```bash
npm start
# або для розробки:
npm run dev
```

Backend буде доступний на **http://localhost:5000**

### 2. Frontend (Next.js React)

```bash
cd frontend
npm install
npm run build
npm start
```

Frontend буде доступний на **http://localhost:3000**

## 📚 Код - Пояснення

### Backend (Node.js)

#### 1. **Що таке сервер?** (`backend/src/server.js`)
Це програма, яка слухає запити від фронтенду (як веб-приклад замовляє напиток у баристи):

```javascript
const app = express(); // Створює сервер
app.listen(5000);      // Слухає порт 5000
```

#### 2. **API маршрути** (як кнопки на банкоматі)
```javascript
app.post('/api/chat', async (req, res) => {
  // req = те, що приходить від фронтенду
  // res = те, що повертаємо назад
});
```

#### 3. **Збереження історії** (Пам'ять)
```javascript
function saveChatHistory(history) {
  fs.writeFileSync(CHAT_HISTORY_FILE, JSON.stringify(history));
  // fs = файлова система
  // Зберігає дані у файл JSON
}
```

#### 4. **Виклик Google API**
```javascript
const response = await model.generateContent({...});
// await = чекаємо відповідь від Google
// response = те, що Google повернув
```

### Frontend (React + Next.js)

#### 1. **State (Стан)** - як пам'ять для сторінки
```javascript
const [history, setHistory] = useState([]);
// history = дані повідомлень
// setHistory = функція для оновлення
```

#### 2. **useEffect** - що робити при завантаженні
```javascript
useEffect(() => {
  fetchHistory(); // Завантажити історію при старті
}, []); // [] = виконати один раз
```

#### 3. **Отправити повідомлення**
```javascript
const handleSendMessage = async (e) => {
  axios.post(`${API_URL}/api/chat`, { message });
  // Надіслати на backend
};
```

#### 4. **Компоненти** - повторні блоки UI
```javascript
<ChatWindow history={history} />
// Це компонент, який показує всі повідомлення
// history = дані, які передаємо всередину
```

## 🎯 Як все працює?

1. Користувач пише повідомлення в **фронтенді** (веб-браузер)
2. React відправляє запит на **бекенд** (Node.js сервер)
3. Бекенд отримує повідомлення, викликає **Google API**
4. Google повертає відповідь AI
5. Бекенд зберігає все у файл (память) і повертає фронтенду
6. Фронтенд оновлює екран - користувач виб бачить відповідь

## 📱 Мобільна підтримка

Фронтенд працює на всіх пристроях (телефон, планшет, комп'ютер) завдяки:
- **Next.js** - оптимізований для мобіля
- **Tailwind CSS** - адаптивний дизайн
- **Responsive layout** - елементи змінюють розмір

## 🗂️ Структура файлів

```
backend/
├── src/
│   └── server.js      ← Основний сервер
├── package.json       ← Залежності
└── .env              ← Налаштування

frontend/
├── app/
│   ├── page.js        ← Главна сторінка
│   ├── layout.js      ← Макет
│   ├── globals.css    ← Стилі
│   └── components/
│       └── ChatWindow.js  ← Компонент чату
├── tailwind.config.js ← Tailwind налаштування
└── package.json       ← Залежності
```

## 🔑 Важні концепції

**Frontend:**
- `useState` = змінна яка змінюється
- `useEffect` = запустити код при змінах
- `async/await` = чекати на результат
- `axios` = відправляти HTTP запити

**Backend:**
- `express` = фреймворк для сервера
- `fs` = робити з файлами
- `async/await` = асинхронний код
- API маршути = адреси де фронтенд запитує

## 🚨 Помилки та рішення

**"Backend offline"** → Запустіть `npm start` в папці backend

**"No API key"** → Додайте GOOGLE_API_KEY в `.env`

**"Port already in use"** → Змініть PORT в `.env`

## 📞 Контакти і допомога

Якщо щось не зрозуміло - питайте! Кожна частина коду пояснюється просто.
